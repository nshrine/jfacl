Edit the jfacl script and set JAVA_HOME to point to your
JRE 5.0 installation and BASE to the directory in which you unpacked jfacl.
Add BASE/bin to your path then run jfacl as:
 
        jfacl [[<file or dir>]...]

