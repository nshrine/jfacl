Make sure JAVA_HOME is set, gcc and javac are in your path then:

cd src
ln -s Makefile.linux Makefile (or Makefile.solaris)
make
make install PREFIX=<install dir>

(installs to /usr/local if you omit PREFIX)

Then run from <install dir>/bin
